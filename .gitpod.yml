image:
    gitpod/workspace-full-vnc
ports:
  - port: 3000
tasks:
  - init: |
      ./gradlew -p language-server build
      yarn --cwd theia
    command:
      yarn --cwd theia/browser-app start --hostname=0.0.0.0 ../../ws-theia
